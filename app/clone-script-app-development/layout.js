export const metadata = {
  title: " On-Demand Mobile Clone Application Development Company",
  description:
    " We are a leading clone app development company offering bespoke mobile clone apps for IOS & Android like Uber, Zomato, Opensea and tinder etc.",
};

export default function Layout({ children }) {
  return <div>{children}</div>;
}
